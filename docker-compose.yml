services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pm-app
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./backend/data:/app/backend/data
